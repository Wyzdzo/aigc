// src/types/common/seo.types.ts

/**
 * Sitemap URL 条目变更频率
 */
export enum ChangeFreq {
  ALWAYS = 'always',
  HOURLY = 'hourly',
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
  NEVER = 'never',
}
