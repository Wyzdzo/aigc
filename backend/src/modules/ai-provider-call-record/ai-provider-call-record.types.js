import { RECORD_SOURCES } from '@app-types/common/record-source.types';
export const AI_PROVIDER_CALL_RECORD_SOURCES = RECORD_SOURCES;
export const AI_PROVIDER_CALL_RECORD_PROVIDER_STATUSES = ['succeeded', 'failed'];
